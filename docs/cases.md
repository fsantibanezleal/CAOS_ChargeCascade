# Cases

The CATEGORY taxonomy + the coverage matrix + one row per documented case live in
**[cases/README.md](cases/README.md)**. Each case carries a category, an operating point, an expected band, a
validation anchor, and a synthetic|control flag; the App shows one case, Experiments/Benchmark summarize across
categories.
