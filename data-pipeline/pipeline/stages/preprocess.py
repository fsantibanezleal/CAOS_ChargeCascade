"""Stage 1 - preprocess (light): validate each case's mill operating point through CONTRACT 1 (pipeline.io.contract). The
cases ARE the inputs; this proves the ingestion gate and carries the flags (phiC>=1, high/low fill, ball/diameter) into
the manifest."""
